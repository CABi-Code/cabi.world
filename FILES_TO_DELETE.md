# Файлы и папки, которые можно удалить

## 🗑️ Полностью лишние (можно удалить сразу)

### 1. Старые маршруты
**Вся папка `index/`** - больше не используется, все маршруты перенесены в `routes/`

```
index/
├── Routes/                    # ❌ Удалить всю папку
│   ├── .PageRoutes.php
│   ├── admin-panel-route.php
│   ├── chat-routes.php
│   ├── modpack-page.php
│   ├── profile-routes.php
│   └── static-routes.php
└── RoutesAPI/                 # ❌ Удалить всю папку
    ├── AdminRoutesAPI.php
    ├── RoutesAPI.php
    ├── switch.php
    ├── application/
    ├── auth/
    ├── chat.php
    ├── community.php
    ├── feed/
    ├── modpack/
    ├── notifications/
    └── user/
```

**Причина**: Все маршруты теперь в `routes/web.php` и `routes/api.php`, обрабатываются через новый роутер.

### 2. Старый AuthManager
**Папка `app/Auth/AuthManager/`** - все трейты объединены в `AuthService`

```
app/Auth/AuthManager/          # ❌ Удалить всю папку
├── ClearTokenCookiesTrait.php
├── GenerateTokensTrait.php
├── GetCurrentUserTrait.php
├── LoginTrait.php
├── LogoutLogoutAllLogAuthTrait.php
├── RefreshTrait.php
├── RegisterTrait.php
└── SetTokenCookiesTrait.php
```

**Файл `app/Auth/AuthManager.php`** - заменен на `app/Services/AuthService.php`

```
app/Auth/AuthManager.php       # ❌ Удалить
```

**Причина**: Вся функциональность перенесена в `app/Services/AuthService.php` без использования трейтов.

### 3. Старые Middleware
**Папка `app/Middleware/`** - дублируются с новыми в `app/Http/Middleware/`

```
app/Middleware/                # ❌ Удалить всю папку
├── AdminMiddleware.php
└── AuthMiddleware.php
```

**Причина**: Новые middleware находятся в `app/Http/Middleware/` и используются роутером.

## ⚠️ Нужно проверить перед удалением

### 1. Старые функции в шаблонах
Проверьте, не используются ли старые функции `json()`, `redirect()` из `bootstrap.php` напрямую в шаблонах. Если используются - оставьте их.

### 2. Глобальные переменные
Убедитесь, что в шаблонах используется глобальная переменная `$user` (она все еще доступна в `public/index.php`).

## 📋 Итоговый список для удаления

```bash
# Удалить старые маршруты
rm -rf index/

# Удалить старый AuthManager
rm -rf app/Auth/AuthManager/
rm app/Auth/AuthManager.php

# Удалить старые middleware
rm -rf app/Middleware/
```

## ✅ Что оставить

- `app/Auth/JWT.php` - используется в `AuthService`
- `app/Auth/login-reserved.txt` - используется в `AuthService`
- Все файлы в `app/Repository/` - используются контроллерами
- Все файлы в `app/Service/` - используются контроллерами
- Все файлы в `templates/` - используются веб-контроллерами

## 🔍 Как проверить перед удалением

1. **Проверьте использование старых маршрутов:**
   ```bash
   grep -r "index/Routes" .
   grep -r "index/RoutesAPI" .
   ```

2. **Проверьте использование старого AuthManager:**
   ```bash
   grep -r "App\\Auth\\AuthManager" .
   grep -r "new AuthManager" .
   ```

3. **Проверьте использование старых middleware:**
   ```bash
   grep -r "App\\Middleware" .
   ```

4. **Протестируйте приложение** после удаления, чтобы убедиться, что все работает.

## 📝 Рекомендации

1. **Сначала сделайте бэкап** или коммит в git
2. **Удаляйте по частям** - сначала `index/`, потом `AuthManager`, потом `Middleware`
3. **Тестируйте после каждого удаления**
4. **Если что-то сломалось** - можно быстро откатить изменения
