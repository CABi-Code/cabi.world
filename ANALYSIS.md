# Анализ проекта cabi.world

## 🔍 Выявленные проблемы

### 1. Архитектурные проблемы

#### 1.1 Отсутствие единого роутера
- **Проблема**: Использование `include_once` вместо роутера
- **Местоположение**: `public/index.php`, `index/Routes/`, `index/RoutesAPI/`
- **Последствия**: 
  - Невозможно централизованно управлять маршрутами
  - Сложно отслеживать зависимости между маршрутами
  - Нет единой точки обработки ошибок
  - Сложно добавить middleware для всех маршрутов

#### 1.2 Смешение слоев
- **Проблема**: Бизнес-логика в файлах маршрутов
- **Примеры**: 
  - `index/RoutesAPI/modpack/apply.php` - содержит валидацию и бизнес-логику
  - `index/Routes/profile-routes.php` - содержит логику проверки файлов
  - `public/index.php` - содержит логику подсчета уведомлений
- **Последствия**: Нарушение принципа единственной ответственности

#### 1.3 Избыточное использование трейтов
- **Проблема**: Слишком много трейтов для разделения больших классов
- **Примеры**:
  - `AuthManager` разбит на 9 трейтов
  - `ChatMessageRepository` разбит на 4 трейта
  - `CommunityRepository` разбит на 6 трейтов
- **Последствия**: 
  - Сложно понять полную структуру класса
  - Трейты скрывают зависимости
  - Усложняет отладку

### 2. Проблемы безопасности

#### 2.1 Недостаточная валидация входных данных
- **Проблема**: Валидация разбросана по разным файлам
- **Примеры**:
  - `index/RoutesAPI/user/update.php` - только `array_intersect_key`, нет проверки типов
  - `index/RoutesAPI/modpack/apply.php` - валидация дублируется в нескольких местах
- **Риск**: Возможность передачи невалидных данных

#### 2.2 Отсутствие rate limiting
- **Проблема**: Нет защиты от злоупотребления API
- **Риск**: Возможность DDoS атак, brute force на логин

#### 2.3 Проблемы с CSRF
- **Проблема**: CSRF проверка только для API, но не для всех форм
- **Местоположение**: `index/RoutesAPI/RoutesAPI.php:30-36`
- **Риск**: Возможность CSRF атак на веб-формы

#### 2.4 Отсутствие валидации файлов
- **Проблема**: Проверка только расширения, нет проверки MIME типа
- **Местоположение**: `app/Service/ImageService.php`
- **Риск**: Возможность загрузки вредоносных файлов

### 3. Дублирование кода

#### 3.1 Повторяющаяся логика обработки FormData vs JSON
- **Проблема**: Одинаковая логика в нескольких файлах
- **Примеры**:
  - `index/RoutesAPI/modpack/apply.php:11-29`
  - `index/RoutesAPI/application/update.php:11-29`
  - `index/RoutesAPI/chat.php:99-120`
- **Решение**: Вынести в отдельный класс `RequestParser`

#### 3.2 Дублирование валидации
- **Проблема**: Валидация сообщений дублируется
- **Примеры**: Проверка длины сообщения в нескольких местах
- **Решение**: Создать класс `Validator` с методами валидации

#### 3.3 Повторяющиеся проверки авторизации
- **Проблема**: `if (!$user) json(['error' => 'Unauthorized'], 401);` повторяется
- **Решение**: Использовать middleware

### 4. Структурные проблемы

#### 4.1 Нестандартная структура папок
- **Проблема**: Папка `index/` не является стандартной для PHP проектов
- **Стандарт**: Обычно используется `routes/` или `app/Http/Routes/`

#### 4.2 Смешение веб и API маршрутов
- **Проблема**: Нет четкого разделения между веб и API
- **Решение**: Разделить на `routes/web.php` и `routes/api.php`

#### 4.3 Отсутствие контроллеров
- **Проблема**: Вся логика в файлах маршрутов
- **Решение**: Создать контроллеры для группировки связанной логики

### 5. Неэффективности

#### 5.1 Множественные запросы к БД
- **Проблема**: N+1 запросы в некоторых местах
- **Пример**: `index/RoutesAPI/chat.php:27-46` - загрузка лайков для каждого сообщения отдельно

#### 5.2 Отсутствие кэширования
- **Проблема**: Нет кэширования статических данных (модпаки, статистика)
- **Решение**: Добавить кэширование через Redis или файловый кэш

#### 5.3 Неоптимальные SQL запросы
- **Проблема**: Использование подзапросов вместо JOIN
- **Пример**: `ApplicationRepository::findByUser()` использует подзапросы для контактов

### 6. Проблемы с обработкой ошибок

#### 6.1 Непоследовательная обработка ошибок
- **Проблема**: Разные форматы ответов об ошибках
- **Примеры**: 
  - `json(['error' => '...'])` 
  - `json(['errors' => [...]])`
  - `json(['success' => false, 'errors' => [...]])`

#### 6.2 Отсутствие логирования ошибок
- **Проблема**: Ошибки не логируются
- **Риск**: Сложно отслеживать проблемы в production

## 📊 Статистика

- **Всего файлов маршрутов**: 25+ файлов
- **Трейтов**: 20+ трейтов
- **Дублирование кода**: ~15% кода дублируется
- **Файлов без контроллеров**: 100%

## ✅ Положительные моменты

1. ✅ Использование prepared statements везде
2. ✅ Правильное хеширование паролей (Argon2ID)
3. ✅ Использование PSR-4 автозагрузки
4. ✅ Типизация (declare(strict_types))
5. ✅ Использование паттерна Repository
6. ✅ Разделение на слои (Repository, Service, Middleware)
