# Очистка старых файлов после рефакторинга

## 📋 Список файлов и папок для удаления

### ✅ Безопасно удалить (больше не используются)

#### 1. Вся папка `index/` (старые маршруты)
```
index/
├── Routes/
│   ├── .PageRoutes.php
│   ├── admin-panel-route.php
│   ├── chat-routes.php
│   ├── modpack-page.php
│   ├── profile-routes.php
│   └── static-routes.php
└── RoutesAPI/
    ├── AdminRoutesAPI.php
    ├── RoutesAPI.php
    ├── switch.php
    ├── application/
    ├── auth/
    ├── chat.php
    ├── community.php
    ├── feed/
    ├── modpack/
    ├── notifications/
    └── user/
```

**Причина**: Все маршруты перенесены в `routes/web.php` и `routes/api.php`

#### 2. Старый AuthManager и его трейты
```
app/Auth/
├── AuthManager.php                    # ❌ Удалить
└── AuthManager/                       # ❌ Удалить всю папку
    ├── ClearTokenCookiesTrait.php
    ├── GenerateTokensTrait.php
    ├── GetCurrentUserTrait.php
    ├── LoginTrait.php
    ├── LogoutLogoutAllLogAuthTrait.php
    ├── RefreshTrait.php
    ├── RegisterTrait.php
    └── SetTokenCookiesTrait.php
```

**Причина**: Вся функциональность объединена в `app/Services/AuthService.php`

#### 3. Старые Middleware
```
app/Middleware/
├── AdminMiddleware.php                 # ❌ Удалить
└── AuthMiddleware.php                  # ❌ Удалить
```

**Причина**: Заменены на новые в `app/Http/Middleware/`

### ✅ Оставить (используются)

```
app/Auth/
├── JWT.php                            # ✅ Используется в AuthService
└── login-reserved.txt                 # ✅ Используется в AuthService
```

## 🗑️ Команды для удаления

### Windows (PowerShell)
```powershell
# Удалить старые маршруты
Remove-Item -Recurse -Force index\

# Удалить старый AuthManager
Remove-Item -Recurse -Force app\Auth\AuthManager\
Remove-Item -Force app\Auth\AuthManager.php

# Удалить старые middleware
Remove-Item -Recurse -Force app\Middleware\
```

### Linux/Mac
```bash
# Удалить старые маршруты
rm -rf index/

# Удалить старый AuthManager
rm -rf app/Auth/AuthManager/
rm app/Auth/AuthManager.php

# Удалить старые middleware
rm -rf app/Middleware/
```

## ⚠️ Перед удалением

1. **Сделайте бэкап или коммит в git:**
   ```bash
   git add .
   git commit -m "Backup before cleanup"
   ```

2. **Проверьте, что новый код работает:**
   - Протестируйте основные функции
   - Проверьте авторизацию
   - Проверьте API эндпоинты

3. **Удаляйте по частям:**
   - Сначала `index/`
   - Потом `AuthManager`
   - Потом `Middleware`

## 🔍 Проверка после удаления

После удаления проверьте:

1. **Авторизация работает?**
   - Вход/выход
   - Регистрация
   - Обновление токенов

2. **API работает?**
   - Все эндпоинты отвечают
   - Middleware применяются правильно

3. **Веб-страницы открываются?**
   - Главная страница
   - Профили
   - Настройки

## 📊 Статистика удаления

- **Папок для удаления**: 3
- **Файлов для удаления**: ~30+
- **Освободится места**: ~50-100 KB (зависит от размера файлов)

## ✅ После удаления

После успешного удаления и проверки:

1. Обновите `.gitignore` если нужно
2. Удалите этот файл (`cleanup-old-files.md`)
3. Обновите документацию если нужно
